{
    "name": "p002-angular-express-webapp",
    "version": "0.0.0",
    "scripts": {
        "ng": "ng",
        "serve": "ng serve",
        "build": "ng build",
        "watch": "ng build --watch --configuration development",
        "test": "ng test --watch=false",
        "start": "node dist/server",
        "build:client:dev": "ng build --configuration=development --deploy-url=pkg/$npm_package_version/web/ && bread-compressor dist/development",
        "build:client:production": "ng build --configuration=production --deploy-url=pkg/$npm_package_version/web/ && bread-compressor dist/production",
        "build:server": "tsc --project tsconfig.server.json",
        "build:dev": "npm run build:client:dev && npm run build:server",
        "build:production": "npm run build:client:production && npm run build:server",
        "start:dev": "npm run start ANGULAR_ENV=development DEPLOYMENT_ENV=development NODE_ENV=development PORT=8080",
        "start:production": "npm run start ANGULAR_ENV=production DEPLOYMENT_ENV=production NODE_ENV=production PORT=8080",
        "serve:dev": "npm run build:dev && npm run start:dev",
        "serve:production": "npm run build:production && npm run start:production"
    },
    "private": true,
    "dependencies": {
        "@angular/animations": "~12.0.1",
        "@angular/common": "~12.0.1",
        "@angular/compiler": "~12.0.1",
        "@angular/core": "~12.0.1",
        "@angular/forms": "~12.0.1",
        "@angular/platform-browser": "~12.0.1",
        "@angular/platform-browser-dynamic": "~12.0.1",
        "@angular/router": "~12.0.1",
        "@types/body-parser": "^1.19.0",
        "@types/compression": "^1.7.0",
        "@types/express": "^4.17.12",
        "@types/express-handlebars": "^5.3.0",
        "body-parser": "^1.19.0",
        "bread-compressor-cli": "^1.1.0",
        "compression": "^1.7.4",
        "express": "^4.17.1",
        "express-handlebars": "^5.3.2",
        "helmet": "^4.6.0",
        "rxjs": "~6.6.0",
        "tslib": "^2.1.0",
        "zone.js": "~0.11.4"
    },
    "devDependencies": {
        "@angular-devkit/build-angular": "~12.0.1",
        "@angular/cli": "~12.0.1",
        "@angular/compiler-cli": "~12.0.1",
        "@types/jasmine": "~3.6.0",
        "@types/node": "^12.11.1",
        "jasmine-core": "~3.7.0",
        "karma": "~6.3.0",
        "karma-chrome-launcher": "~3.1.0",
        "karma-coverage": "~2.0.3",
        "karma-jasmine": "~4.0.0",
        "karma-jasmine-html-reporter": "^1.5.0",
        "typescript": "~4.2.3"
    }
}
